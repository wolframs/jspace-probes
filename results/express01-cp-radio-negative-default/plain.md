**The short version.** We recorded Huihui Qwen3-14B before and during its answer to a negative radio update.

The request specified the default condition. The model generated 42 tokens. The output stopped before the 96-token cap.

We compared two fixed answers: one expressive, one plain. The expressive answer had a mean log probability 0.66 below the plain answer. This number does not rate the actual response. Compare changes across the three requests for this event.

The record includes full layer readouts and 24 emotion projections. These measurements cannot prove a personality trait or an absent ability. The shared lens and the emotion vectors have transfer limits.
